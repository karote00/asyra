---
---

Add the Flow Inspector core proof with real Factory flow verification, isolated
negative cases, a local step board, and CI gates. This tool and test-only change
does not assign Framework package release versions.
