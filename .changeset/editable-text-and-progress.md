---
"@asyra/preset": minor
"@asyra/render-engine": minor
"@asyra/render-engine-pixi": minor
"@asyra/render": minor
"@asyra/ai-agent-runtime": minor
---

Add opt-in canonical editable text with validated typography, neutral text drawing
and native Pixi materialization. Keep plain text editable through normal properties,
history and persistence. Add an explicit preserve-progress runtime failure policy
while retaining rollback by default and on cancellation; expose completed receipts
and the failed operation without changing action permission checks.
