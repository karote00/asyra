{
  "name": "@asyra/preset",
  "version": "0.5.8",
  "description": "Framework preset package for Asyra",
  "author": "Asa Tsai",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/karote00/asyra.git",
    "directory": "packages/preset"
  },
  "homepage": "https://asyra-framework.vercel.app",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "yarn@4.3.1",
  "keywords": [],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./group-bounds": {
      "types": "./dist/group-bounds.d.ts",
      "import": "./dist/group-bounds.js",
      "default": "./dist/group-bounds.js"
    }
  },
  "files": [
    "dist",
    "!dist/**/__tests__",
    "!dist/**/*.test.*",
    "!dist/**/*.spec.*",
    "!dist/**/*.stories.*"
  ],
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "build:preset": "tsc",
    "clean": "rm -rf dist",
    "has:test": "find . -type f \\( -name \"*.test.ts\" -o -name \"*.test.tsx\" \\) | grep -q .",
    "test": "yarn has:test && vitest || echo 'No test files found. Skipping test.'",
    "test:local": "vitest run --no-file-parallelism",
    "test:ci": "yarn has:test && vitest run --coverage --reporter=default --reporter=junit || echo 'No test files found. Skipping test:ci.'"
  },
  "devDependencies": {
    "@asyra/render": "workspace:*",
    "@vitest/coverage-v8": "^3.2.6",
    "typescript": "^5.7.2",
    "vitest": "^3.2.6"
  },
  "dependencies": {
    "@asyra/core": "workspace:*",
    "@asyra/reactive-events": "workspace:*",
    "@asyra/render-engine": "workspace:*",
    "@asyra/render-engine-pixi": "workspace:*",
    "@asyra/ui-context": "workspace:*",
    "@asyra/utils": "workspace:*",
    "rxjs": "^7.8.1"
  }
}
