/** Current portable envelope identities. Versions remain independently validated. */
export const StorageFormats = {
  PROJECT: 'sim-project',
  TRAJECTORY: 'sim-trajectory',
  OBSERVATIONS: 'sim-observations',
  RUN_REPORT: 'sim-run-report'
} as const
